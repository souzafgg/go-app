---
- name: Testing hosts
  hosts: main
  gather_facts: false

  tasks:
    - name: Check if the page is ok
      uri:
        url: http://{{ inventory_hostname }}
        method: GET
        status_code: 200
